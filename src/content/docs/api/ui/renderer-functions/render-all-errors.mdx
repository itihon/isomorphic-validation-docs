---
title: renderAllErrors() function
description: Renders all validation error messages of a validation result.
sidebar: 
    label: renderAllErrors()
    badge:
        text: 🧪
        variant: caution
---

import Playground from '../../../../../components/Playground.jsx';
import exampleJS from '../../../../../../public/playground/api/ui/renderer-functions/render-all-errors/example.js?raw';
import exampleHTML from '../../../../../../public/playground/api/ui/renderer-functions/render-all-errors/example.html?raw';
import exampleCSS from '../../../../../../public/playground/api/ui/renderer-functions/render-all-errors/example.css?raw';

Creates a renderer function that renders all validation error messages of a validation result.

## Syntax

```js 
renderAllErrors()
renderAllErrors(errorPropName)
renderAllErrors(errorPropName, toString)
```

### Parameters

- `errorPropName` <sup>optional</sup>
<br/> A property name of an error message supplied with a predicate function as `anyData` when adding as a [constraint](/api/validation/instance-methods/constraint/#parameters).
<br/> **Default**: `'msg'`.

- `toString` <sup>optional</sup>
<br/> A function that accepts a validation result entry with the property name in it, index, and an `Array` of the entries and returns a string.
<br/> **Default**: implementation: [render-entry.js](https://github.com/itihon/isomorphic-validation/blob/master/src/helpers/render-entry.js).

### Return value

A function that accepts a [`ValidationResult`](/api/validation-result/) object and returns a string. 

## Description

Creates a function that is intended to be used as the `value` property in the `stateValues` object which is passed as a parameter into the [`applyBox()`](/api/ui/effect-functions/apply-box/#parameters) function.

<br/>**Implementation:** [render-all-errors.js](https://github.com/itihon/isomorphic-validation/blob/master/src/ui/render-all-errors.js).

## Examples

<Playground client:visible  
    params={{
        console: 'closed',
        js: exampleJS,
        html: exampleHTML,
        css: exampleCSS,
    }}
    height="500px"
    width="100%"
/>